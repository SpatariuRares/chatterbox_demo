# Chatterbox TTS Studio - Requirements
# Version 1.1.3

# IMPORTANT: This project is tested with Python 3.8 - 3.11
# Python 3.13 may have compatibility issues with some dependencies
# If you encounter errors, try using Python 3.11 instead:
#   py -3.11 -m venv venv

# Core Dependencies
# IMPORTANT: Install requirements with CUDA support using:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.24.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0

# Web Interface
gradio>=4.0.0

# Chatterbox TTS (install from local package)
# Run: pip install -e ./chatterbox

# ML/AI Libraries
transformers>=4.30.0
diffusers>=0.20.0

# Optional: For better performance
# peft>=0.5.0  # Removes LoRA deprecation warning

# System Dependencies (install via system package manager)
# - ffmpeg (for MP3 conversion)
#   Windows: Download from https://ffmpeg.org/download.html
#   Linux: sudo apt install ffmpeg
#   Mac: brew install ffmpeg
